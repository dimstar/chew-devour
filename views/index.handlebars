<div class="col-sm">
    <ul class="burger-list list-group list-group-flush">
    {{#each burgers}}
        {{#unless devoured}}
            <li id="burger-{{id}}" class="list-group-item">{{name}} <button data-burger="{{id}}" class="devourIt btn btn-secondary btn-sm">devour!</button></li>
        {{/unless}}
    {{/each}}
    </ul>
</div>
<div class="col-sm">
    <ul class="burger-list list-group list-group-flush">
    {{#each burgers}}
        {{#if devoured}}
            <li id="burger-{{id}}" class="list-group-item">{{name}}</li>
        {{/if}}
    {{/each}}
    </ul>
</div>
    